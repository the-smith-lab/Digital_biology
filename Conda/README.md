

To install all software for the class project:

1. Delete your entire Conda environment:

   ```
   rm -rf Miniconda/
   ```

2. Copy/download `class_project`, and run the below command:

`mamba env create -f Conda/class_project.yml`
